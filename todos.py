"""
Project todo entries (backlog items as plain strings).

Prompt: add todos.py with list entries:
    link graph entries over layer,

Prompt: add todo "safety.py intelligent prevent drug design"
"""
from __future__ import annotations

# Backlog lines — extend this list as new work items are agreed; keep one actionable phrase per entry.
TODOS: list[str] = [
    "link graph entries over layer",
    "safety.py intelligent prevent drug design",
]

__all__ = ["TODOS"]
